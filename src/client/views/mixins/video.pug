mixin video(info)
  div
    h4
      a(href=`/videos/${info.id}`)=info.title
    p description: #{info.description} 
    small date : #{info.createdAt}
    ul
      each h in info.hashtags
        li=h
