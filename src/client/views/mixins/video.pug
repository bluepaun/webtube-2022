mixin video(info)
  div
    h4
      a(href=`/videos/${info.id}`)=info.title
    ul
      li id: #{info.id}
      li description: #{info.description} 
      li date : #{info.createAt}
      li hashtags: #{info.hashtags}
      li meta: #{info.meta}
